# Eval: project-bootstrap

Fixtures and prompts for evaluating `ds-project-bootstrap`.

## Status

Stub — add cases that check:

- Correct folder layout created
- README + contract stubs present
- `.gitignore` excludes raw data and venvs
